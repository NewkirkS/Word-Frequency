<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <meta charset="utf-8">
        <link rel="stylesheet" href="styles/styles.css">
        <title>Word Frequency Search</title>
    </head>
    <body>
        <h1>Word Frequency</h1>
        <hr>
        {% if  repeat_counter.getMatches == 0 %}
            <h3>No matches found for the word "{{ repeat_counter.getSearchWord }}" in the text</h3>
            <hr>
            <a href="/">Back</a>
        {% elseif  repeat_counter.getMatches == 1 %}
            <h3>1 match found for the word "{{ repeat_counter.getSearchWord }}" in the text</h3>
            <hr>
            <a href="/">Back</a>
        {% else %}
        <h3>{{ repeat_counter.getMatches }} matches found for word "{{ repeat_counter.getSearchWord }}" in the text</h3>
        <hr>
        <a href="/">Back</a>
        {% endif %}
    </body>
</html>
